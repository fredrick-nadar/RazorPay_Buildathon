"""Importers: typed source adapters and dataset normalization (PRD 8.1).

Runtime import code reads only dataset ``inputs`` directories. It never
imports the evaluator package and never reads ground-truth data.
"""
